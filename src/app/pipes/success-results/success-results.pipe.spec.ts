import { SuccessResultsPipe } from './success-results.pipe';

describe('SuccessResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new SuccessResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
